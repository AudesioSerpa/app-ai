node_modules/
.gradle/
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/keystore.properties
android/app/release.keystore
android/app/debug.keystore
*.jks
*.keystore
*.log
.DS_Store

# --- Gradle wrapper: garante que o .jar do wrapper NUNCA seja ignorado ---
# (ferramentas às vezes filtram *.jar por padrão; a linha abaixo força a inclusão)
!android/gradle/wrapper/gradle-wrapper.jar
